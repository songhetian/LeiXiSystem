# 🚀 雷犀旗舰系统 - 部署与运维使用说明

本手册旨在指导开发者完成从本地构建到云端生产环境部署的全流程操作，并提供针对 `502 Bad Gateway` 及 `Socket` 连接失败的专项方案。

## 📋 目录
1. [本地构建优化](#1-本地构建优化)
2. [核心配置文件说明](#2-核心配置文件说明)
3. [阿里云 RDS 白名单配置](#3-阿里云-rds-白名单配置)
4. [服务器部署路径映射](#4-服务器部署路径映射)
5. [后端服务管理 (PM2)](#5-后端服务管理-pm2)
6. [Nginx 代理配置](#6-nginx-代理配置)
7. [常见问题排查 (502/Socket)](#7-常见问题排查)

---

### 1. 本地构建优化
在将代码上传服务器前，必须在本地执行构建。

*   **命令**：`npm run build`
*   **优化项**：
    *   **分包策略**：系统已配置 `Vite` 自动将 `Ant Design`、`ExcelJS`、`Charts` 等大体积库拆分为独立 Chunk。
    *   **性能提升**：通过异步动态导入，首屏加载体积减少了约 **1.5MB**。
    *   **产物目录**：构建完成后，所有静态资源将生成在 `dist-react/` 文件夹中。

---

### 2. 核心配置文件说明
这是系统运行的“心脏”，格式错误会导致后端直接崩溃。

*   **路径**：`config/db-config.json`
*   **关键字段解析**：
    *   `database`: 填入阿里云 RDS 的连接地址、账号、密码及库名。
    *   `redis`: 必须填写正确的密码。Socket 通讯极度依赖 Redis。
    *   `upload`:
        *   `sharedDirectory`: 服务器上存储上传文件的**物理路径**（如 `/www/wwwroot/leixi-system/uploads`）。
        *   `publicUrl`: 前端访问 API 的基础路径。

⚠️ **注意：** JSON 格式严禁在最后一个属性后添加逗号（`,`），否则后端会报 `SyntaxError` 并关停 Socket 服务。

---

### 3. 阿里云 RDS 白名单配置
由于使用的是阿里云 RDS，默认拒绝所有连接。

1.  登录 **阿里云控制台** -> **云数据库 RDS 版**。
2.  进入实例，点击左侧 **“白名单与安全组”**。
3.  **必须操作**：将你服务器的 **公网 IP** 添加到白名单中。
4.  *测试环境可用 `0.0.0.0/0`，生产环境建议仅填服务器 IP。*

---

### 4. 服务器部署路径映射
建议的生产环境目录结构：

```text
/www/wwwroot/leixi-system/
├── dist/               # 存放本地 dist-react 里的内容 (前端)
├── server/             # 存放后端源码 (Node.js)
├── config/             # 存放 db-config.json
├── uploads/            # 存放上传的文件 (需 777 权限)
└── package.json        # 用于 PM2 启动和依赖管理
```

---

### 5. 后端服务管理 (PM2)
后端使用 PM2 进行进程守护和自愈。

*   **启动服务**：`pm2 start server/index.js --name "leixi-system"`
*   **重启服务**：`pm2 restart leixi-system`
*   **查看实时日志**：`pm2 logs leixi-system`
*   **状态检查**：`pm2 status`

---

### 6. Nginx 代理配置
确保 Nginx 能正确转发 API 和 Socket 请求。

```nginx
location /api/ {
    proxy_pass http://127.0.0.1:3001/api/;
    proxy_set_header Host $host;
}

location /socket.io/ {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "Upgrade";
}
```

---

### 7. 常见问题排查
#### Q: 页面显示 502 Bad Gateway？
*   **原因 1**：后端进程崩了。执行 `pm2 logs leixi-system` 查看。
*   **原因 2**：`db-config.json` 语法错误。
*   **原因 3**：数据库白名单未加，导致后端启动时连接超时退出。

#### Q: Socket 服务器无法使用/实时通知不亮？
*   **原因**：后端未正常启动。Socket 是集成在后端 3001 端口上的，后端一旦崩溃（Crash），Socket 会同步失效。请优先修复后端。

#### Q: 加载速度仍然很慢？
*   **方案**：在 Nginx 配置中开启 `gzip on;`，这能将打包后的 JS 体积再压缩 70%。
