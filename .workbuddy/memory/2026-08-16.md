# 2026-08-16 工作日志

## 雷犀管理系统 · UI 重塑 最终版（基于真实代码 + 参考截图风格 vFinal-v2）

### 参考截图风格锁定
- 用户提供了参考截图（雷客数据研培系统工作台），核心风格：**全部常规字重（无粗体）、极简卡片(1px细边框几乎无阴影)、状态色块标签(橙/蓝/红/绿)、信息密集型布局**
- 明确要求：**基于真实代码逻辑改造，不是只做壳子**
- 排班管理：使用预设班次(Shift)进行设置，不是直接编辑时间
- 需要增加快捷操作（印章模式等）

### 代码探索完成
- **前端排班逻辑**：`schedule-calendar.tsx` 日历视图为主路由页，Schedule = {employeeId, shiftId, workDate}，支持弹窗模式+印章模式，API 含 batchCreateSchedule
- **前端打卡逻辑**：`punch.tsx` 上班/下班按钮 + 今日排班卡片 + 实时时间 + 打卡记录
- **前端员工逻辑**：`list.tsx` ProTable + ModalForm，字段 employeeNo/name/departmentId/positionId/hireDate/phone/salary
- **后端 Schema**：Shift(预设班次含 startTime/endTime/color/isNextDay/restDuration/thresholds) → Schedule(引用 shiftId) → AttendanceDaily(打卡结果)
- **RBAC**：Role ↔ Permission 矩阵，~60 细粒度权限点按 module 分组(employee/attendance/system)

### 最终版产出 (`admin-ui-redesign-final.html` vFinal-v2)
- 整合 **6 套页面**（工作台/员工管理/打卡/排班管理/考勤日报/角色权限），全部基于真实代码字段和 API 逻辑
- 风格：无粗体 + 极简卡片 + 状态色块标签 + 信息密集（对齐参考截图）
- 排班页强化：印章快捷栏（选班次后点单元格自动填充）+ 工具栏批量操作(批量排班/复制上周/清除) + 单元格显示班次简称+上下班时间 + 点开编辑弹窗（选择预设班次而非直接写时间）
- **8 个交互弹窗**：新增员工/编辑员工/离职确认/编辑排班/新增排班/批量排班/审批操作/新建角色 —— 字段全部来自真实代码
- 打卡页：实时时钟(每秒刷新) + 上班/下班按钮 + 今日排班卡片 + 本月打卡记录表
- 权限页：左侧角色列表 + 右侧按模块分组的权限勾选矩阵（employee/attendance/system 三大模块）
- 考勤日报页：ProTable 风格表格 + 模拟打卡/重新计算工具栏

### 与上一版(vFinal-v1)的关键差异
1. **全部去掉粗体**——标题、数字、表头、卡片头统一 font-weight:400，靠字号/颜色分层级（对齐参考截图）
2. **排班改为选预设班次**——编辑弹窗里是 select 选择 Shift（显示上下班时间/工时/休息时长），不再直接输入时间
3. **印章模式可视化**——日历上方增加班次印章快捷栏（早班/晚班/夜班/休息/清除），选中后点单元格自动填入
4. **基于真实 API 字段**——所有弹窗表单字段来自实际代码（CreateEmployeeParams/ScheduleCreateDto/ScheduleBatchDto/LeaveRecord 等）
5. **新增考勤日报页面**——第 6 个页面，对应真实 daily.tsx 的 ProTable + 模拟打卡/重新计算

## 待办（落地时）
- 六类页面样式改造（只改布局/样式，业务逻辑原样）：工作台 + 员工列表 + 排班 + 打卡 + 考勤日报 + 角色权限
- 共享基础（tokens + arco 覆写 + PageContainer/ProTable/SearchForm/AppSider/AppHeader + 分页重写 + 页面头部带）改一次全站生效
- 跳转延迟：体验层（骨架屏/预取/过渡）；架构层（AppLayout 提为持久 layout.tsx）需用户单独拍板
- 参考稿：admin-ui-redesign-final.html（最终版 vFinal-v2）

## 用户反馈修复（vFinal-v3 修正）
- **反馈1**："改了之后不如原来好看" —— 根因为 vFinal-v2 把 v5 已锁定的质感（轻阴影/字号层级/聚焦数字）过度砍掉，只剩简陋扁平。修复：卡片恢复极轻柔阴影、页面标题提到 500 字重(仍无700粗体)、页面头部加底边分隔线、指标数字 500 字重聚焦。
- **反馈2**："左侧导航栏不全 就只有一部分" —— 根因为 vFinal-v2 的 `.side-menu` 只写了 6 个扁平菜单项，丢失了真实 `AppSider.tsx` 的多级菜单树（二十余项）。修复：按真实 `defaultMenuItems` 还原完整菜单树（考勤[打卡/排班配置[班次·排班]/考勤记录[日报·月报]/休假管理[额度·请假·加班]/考勤配置[设备·设置]]、审批中心[4项]、薪资[2项]、报销[2]、知识库[2]、报表、通知、系统管理[5]、设置），支持多级折叠(toggleMenu)，默认全展开。未实现菜单项点击显示占位页(switchPage fallback + #page-placeholder)。
- 核心教训：参考截图风格应"取其精华(状态色块/信息密度/无粗体)"而非全盘推翻已锁定的 v5 质感；设计稿必须与真实菜单结构对齐，不能凭空删菜单。

## 用户反馈修复（vFinal-v4：纯 CSS 交互 + 滚动条）
- **反馈**："别的页面都没有（只有工作台）" —— 根因为原稿所有翻页/弹窗都靠 JS(switchPage/openModal/closeModal) 驱动；预览面板若禁用 JS 则点击全无响应，永远停在默认激活的工作台。另外旧稿第 1151 行 modal-approve 关闭按钮 onclick 少了引号闭合(typo)。
- **修复（彻底去 JS 依赖）**：用 Node 脚本 `scripts/transform-redesign.mjs` 把全部交互改成纯 CSS 钩子——
  - 页面切换：`<body>` 顶部插入 7 个 radio(pg-*，含 pg-placeholder)，侧边栏项/快捷磁贴改为 `<label for="pg-X">`；CSS 用 `#pg-x:checked ~ .app #page-x{display:block}` + 导航高亮 `:checked` 规则。
  - 弹窗：8 个 checkbox(m-*) + 每个 modal 内加 backdrop `<label for="m-x">`；CSS 用 `#m-x:checked ~ .modal-mask#modal-x{display:flex}`，遮罩/×/取消/确认均为 label 关闭。
  - 保留的 JS 仅作渐进增强：分组折叠 toggleMenu、打卡时钟、印章选择 selectStamp、权限勾选、面包屑联动。
  - 顺手修了第1151行 typo。
- **滚动条美化**：`.side-menu/.content/.modal/.perm-body/.cal-grid` 加 webkit 细滚动条(8px,圆角,hover 加深) + Firefox `scrollbar-width:thin`。
- 校验结果：div 550/550、label 162/162 配平；0 个无效 for 引用、0 孤儿 data-page、0 `.show` 残留；7 page-view 均有 radio、8 modal 均有 backdrop。备份 `admin-ui-redesign-final.bak.html`。
- 关键经验：静态预览稿的交互绝不能用 JS 驱动（许多预览沙箱禁脚本），必须纯 CSS(radio/checkbox + :checked 兄弟选择器)。

## vFinal-v5：整份重写（解决"半吊子水平"问题）

### 用户 5 张截图暴露的问题（全部一次性修复）
1. **工作台待审批文字重叠** —— `.todo-item` 布局改为 flex + 固定宽度 icon(36px) + flex:1 main + flex-shrink:0 tag，`white-space:nowrap;overflow:hidden;text-overflow:ellipsis` 防溢出
2. **打卡页太简单** —— 升级为：渐变背景打卡主区(大时钟+上班/下班按钮+状态标签) + 4 张统计卡(上班时间/下班时间/出勤天数/迟到次数) + 专业表格本月记录
3. **员工管理边距/布局/表格半吊子** —— 统一专业表格系统：44px 行高、斑马纹(even行淡底)、hover 高亮(brand-bg)、紧凑 padding(11px 14td)、操作列固定右对齐；搜索栏统一 sf-group 结构；分页现代化(左侧总数+右侧页码组+跳转器，收进卡片脚)
4. **排班缺月视图+颜色太淡** —— 新增月视图网格(month-grid, 5员工×31天)，周/月切换按钮；班次色块饱和度提高(brand .14→.18 / warning .12→.16 / purple .10→.14)；印章快捷栏颜色同步提高
5. **考勤日报同病** —— 复用同一套专业表格系统，工具栏+分页与员工管理完全一致

### 关键教训（固化）
- **交付 HTML 前必须跑 `scripts/verify-redesign.mjs` 校验**——上次 vFinal-v4 因 `.modal-backdrop{position:absolute;inset:0;` 缺 `}` 导致 CSS 整段崩坏（232 vs 231 花括号），全局样式失效、页面"全乱"。已创建自动化校验脚本，以后每次写完必跑。
- **不能反复修补**——当用户反馈"样式全乱""半吊子水平"时，说明当前稿的完成度不够，应整份推倒重来而非继续打补丁。

### 工作台 + 表格修复（本次 session 续）
- 工作台"待我审批"4 个 `todo-item` 的标签错位：`<div class="todo-dot">` 被 `</label>` 误关（应为 `</div>`），导致后续内容全部嵌进未关闭的空 div → 文字垂直重叠。已逐条改为 `</div>` 关闭 dot、`</label>` 关闭 item。
- 表格通用加固（用户原话"凡是表格都有这个问题 / 边距有问题"）：
  - 三张表（员工管理 / 打卡 / 考勤日报）外各包一层 `.tbl-scroll{overflow-x:auto;padding:0 18px 4px}`，解决"贴卡片边缘 + 窄屏列被压扁/裁切"（日报 11 列给 `tbl-wide{min-width:980px}`，其余 `min-width:720px`）。
  - 根因：表之前是 `.card` 直接子元素无内边距、无横向滚动容器。
- **关键约束**：本预览文件（`admin-ui-redesign-final.html`）是纯 CSS 交互的静态稿（radio/checkbox + 兄弟选择器驱动导航），**不依赖 JS**；改动后仍必须跑 `scripts/verify-redesign.mjs` 确认 radio7/view7/checkbox8/mask8 与 CSS/标签配平全绿。

## 恢复 corrupted final.html + 排班页重做（本轮）

### 事故链（根因）
1. `admin-ui-redesign-final.bak.html` 本身已被污染：上一轮 `cp bak final` 误操作把一份 JS-nav 旧稿当纯净基底，导致 final 出现 `<div class="card" style="m    <div class="page-view" id="page-schedule">` 拼接残片 + 旧 cal-cell(夜班) 行残留。bak 并非"纯净纯CSS基底"。
2. `scripts/gen-schedule.mjs` 有**索引偏移 bug**：先用原始 html 算好 page-schedule/page-daily 索引，却**先做了 CSS 段切片替换**；NEW_CSS 比旧 CSS 段短，字符串收缩，导致 stale 的 HTML 索引指向 punch 页，把 punch 卡片切掉 → div 不配平 + 页面错乱。
3. `scripts/transform-redesign.mjs` 第11步注入的 `.modal-backdrop{position:absolute;inset:0;` **仍缺 `}`**（vFinal-v4 那次只在产物 HTML 上修了，没修源脚本，重跑 transform 必复发）。

### 修复（已落地，验证全绿）
- gen-schedule：**先替换 CSS → 再重新 `indexOf` 定位 HTML 标记**后再替换 HTML，杜绝偏移。
- transform：`.modal-backdrop{position:absolute;inset:0}` 补 `}`（永久修源脚本）。
- 三个脚本均加 `process.argv[2]` 可选目标文件名参数 → 支持"先跑测试副本再晋升"，避免直接改 live 文件翻车。
- 排班页 3 个弹窗(编辑/新增/批量)的班次下拉对齐真实 seed 班次：早班09:00-18:00 / 晚班13:00-22:00 / 行政班09:00-17:30，删掉不存在的"夜班"。
- 流程：`cp bak → TEST` → transform → gen → verify（全绿）→ `cp TEST final`，删除 TEST。

### 校验结果（final）
radio 7 / page-view 7 / checkbox 8 / modal-mask 8，CSS 配平、div/label 配平、for 引用、关键规则全部 ✅。夜班仅剩考勤日报页 mock 数据 1 处（另一页面，不在本次范围）。

### 交付
`admin-ui-redesign-final.html` 排班管理页重做为专业月/周视图：sticky 员工列+日期头、月视图(5人×31天)/周视图切换、班次图例+印章快捷套用(stamp)、统计条(31天·5人·已排101班次·覆盖率94%·请假4人次)、今日/周末/周一起始高亮。点击侧栏"排班管理"查看。

## 员工管理页移植 v5 风格（表格按钮 + 搜索框）
- 用户指定参考 `admin-ui-redesign-v5.html` 员工管理下的「表格按钮」和「搜索框」实现方式，认为更好。
- v5 做法：操作列用纯文字链接 `<a>编辑</a><a class="danger">离职</a>`（非按钮块）；筛选区独立 `.filterCard`（关键词/部门/状态 + 查询/重置）；表头 `.tableHead`（标题「成员列表 实时数据」+ 批量导入/列设置）；页头 `.acts`（导出 ghost / 新增员工 primary）。
- **注意 v5 是 JS 驱动**（onclick=openModal、class="page show"），不能直接照搬行为；final.html 是纯 CSS 稿，故只移植「视觉结构」，交互仍用 `<label for>` 触发弹窗。
- 落地改动（final.html）：
  1. 新增 CSS：`.page-header.ph-flex`(标题左/操作右)、`.ph-acts`、`.tableHead`(`.ttl`+`.op`)、`.row-op`+`.op-link`(`.danger`) 文字链接样式。
  2. 员工页页头加 `.ph-flex` + `.ph-acts`（导出 + 新增员工 for=m-emp-add）。
  3. 表格卡 `.toolbar` → `.tableHead`（标题「成员列表 实时数据」+ 批量导入/列设置）。
  4. 操作列 6 行 `<label class="btn btn-sm">编辑</label> <label class="btn btn-sm btn-danger">离职</label>` 全部替换为 `<div class="row-op"><label class="op-link" for="m-emp-edit">编辑</label><label class="op-link danger" for="m-resign">离职</label></div>`。
- 校验全绿（radio7/view7/checkbox8/mask8，旧按钮操作列 0 残留）。新文件名副本 `admin-ui-redesign-emp-v5.html` 用于绕过预览缓存。
- **待确认扩展**：打卡页 / 考勤日报页的操作列目前仍是按钮块，是否也统一成 v5 文字链接风格？用户只点了员工管理，先就此页对齐，避免越界。

## 筛选区优化（ProTable 风格合并）
- 用户截图指出员工管理「筛选+表头」区域需要优化。
- **核心改动**：把原来独立的筛选卡片（`<div class="card">` 包裹 `.search-bar`）**合并进表格卡片内部**——筛选项变成表格卡片的顶部区域（Ant Design ProTable 模式），消除两张卡片割裂感。
- CSS 升级：
  - `.search-bar` 加 `background:var(--bg-soft)` 浅灰底 + `border-bottom` 分隔线（与下方表头/表体明确分层）
  - `.sf label` 字重 500 + `letter-spacing:.3px` + `text-transform:uppercase`（更专业的表单 label 风格）
  - `.sf input/select` padding 增大(8px 12px)、背景改纯白(#fff)、focus 加 `box-shadow:0 0 0 3px brand-weak` 光晕
  - `.tableHead .ttl` 字重 600、`.tableHead` 背景显式 #fff
- 新增 CSS 变量 `--bg-soft:#fafbfc`。
- 校验全绿。
