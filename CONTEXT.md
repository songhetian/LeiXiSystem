# CONTEXT.md — 雷犀客服管理系统 · 项目上下文

> 本文件是项目领域上下文与术语表的唯一入口。任何会话开始前先读本文件。
> 架构决策见 `docs/adr/`；任务清单见 `docs/tickets/`；重构总纲见 `REFACTOR_PLAN.md`。

## 项目背景

- **产品**：雷犀客服管理系统——企业内部管理后台，核心业务为**考勤 → 算薪**闭环，另含员工、报销、培训、公告、审批等模块。
- **当前状态**：2026-08-12 起进入重构（技术栈换血），旧代码归档于 `legacy/`，新架构从零搭建。
- **目标形态**：纯 Web（浏览器访问），放弃 Electron 桌面端。

## 技术栈（已拍板）

- 前端：Next.js (App Router) + TypeScript + **Arco Design Pro** + Refine（内置 React Query）+ bizcharts + zustand
- 后端：NestJS (platform-fastify) + **Prisma** + MySQL + Zod + JWT/RBAC + Socket.IO + Schedule + pino
- 基础设施：阿里云 RDS（生产库）+ 阿里云 OSS（对象存储）；Redis 按需
- 工程：pnpm workspace 单仓库（apps/web + apps/server + packages/shared）；UI 规范强制见 REFACTOR_PLAN.md 第 11 章

> ✅ 技术栈及业务规则全部决策已于 2026-08-12 确认（见 `docs/pending-decisions.md`，已转正为决策记录；ADR-0001~0010 全覆盖）。

## 业务规则（已确认，摘要）

### 薪资
- **加班费**：平日 1.5 / 休息日 2 / 法定节假日 3 倍（按小时；**小时基数 = 基本工资 ÷ 21.75 ÷ 8**；时长 = min(申请, 实际超出下班后打卡)）
- **缺勤扣款**：基本工资 ÷ 当月应出勤天数 × 缺勤天数；入职/离职按实际出勤折算；**请假天数计入缺勤扣款（统一口径）**
- **全勤奖**：当月无迟到/缺卡/请假（有薪假除外）
- **调休转换**：1:1；社保/公积金按档案固定金额代扣
- **跨天班次加班**：按班次结束时间判定，超时分钟数计加班（原型已验证）
- **算薪流程**：试算 → 确认（抽检 3 人）→ 发布；人工调整走 `payroll_adjustments` 留痕；发布撤回仅限未查看，已查看走补发/更正
- **算薪输入 = 已确认的考勤月报快照（ADR-0011）**：不读实时打卡数据；**结账前**漏卡/迟到走补卡审批修正日报（随时可改），**结账后**发现的例外不回头改月报，走工资调整项修正（工资 = 锁定快照 + 调整项）

### 考勤
- 跨天班次按班次归属日归集；同日取首次为上班卡、最后一次为下班卡（>4 次标记异常）；补卡走审批流并留痕

### 权限
- RBAC（菜单/按钮）+ **部门数据隔离**（超管/HR 全量、部门经理本部门含子部门、员工仅本人），后端过滤

### 数据保留
- 打卡流水 3 年；考勤月报与工资条永久

## 领域术语表

> 定义一行搞定（是什么，不是做什么）；**加粗**表示该术语与其他术语的关系。

### 组织与人员

- **员工**：系统内的在职/离职人员，通过 **工号** 唯一标识；关联 **用户**（登录账号）、**职位**、**部门**。
- **用户**：可登录系统的人，关联一个或多个 **部门**、一个或多个 **角色**。
- **部门**：组织的树形结构单元；**员工** 归属其中。
- **职位**：岗位定义（如客服、组长），员工任其职。
- **角色**：RBAC 中的权限集合载体，通过 **角色-权限** 绑定决定可见功能。

### 考勤与工时

- **考勤**：对员工出勤情况的记录与统计，是 **算薪** 的主要输入。
- **打卡机**：考勤数据采集设备——**已确认型号：熵基科技 XFace600**（支持局域网 HTTP API 增量拉取 + U 盘 CSV 导出）；采集层以 HTTP API 为主、CSV 导入兜底，把原始打卡数据送入系统。
- **打卡流水**（punch_logs）：打卡机产生的**原始记录**（员工、时间、设备），未经业务解释。
- **班次**：预定义的上下班时间规则（如早班 8:00-17:00），**排班** 将班次分配给员工和日期。
- **排班**：员工 × 日期 × 班次 的分配关系；**考勤规则引擎** 依据排班解释打卡流水。
- **迟到 / 早退 / 缺卡 / 加班**：考勤规则引擎对打卡流水与班次比对后产生的考勤结果类型；**加班** 同时关联 **加班申请** 审批。
- **请假**：员工发起、经 **审批流** 批准的休假记录；按类型（事假/病假/年假等）关联 **休假额度**。
- **休假额度**：各 **休假类型** 下员工可用的天数余额，随请假/调休增减。
- **调休 / 加班转换**：加班时长转换为休假额度或补偿的规则。

### 薪资

- **薪资项目**：工资构成的最小单元（基本工资/加班费/缺勤扣款/全勤奖/补贴/代扣等），每项有计算规则（固定/按天/按小时/公式）。
- **工资计算批次**（payroll_runs）：某个月度的一次算薪执行，状态为 草稿 → 已确认 → 已发布。
- **工资条**：发给员工的当月工资明细（每人一条），员工自助查看；金额明细来自 **薪资项目** 计算结果。

### 协同与审批

- **审批流**：业务（请假/加班/报销/员工异动）的审批链路定义，按 **角色** 或 **审批组** 路由。
- **审批组**：一组可充当审批人的用户集合，可被多个 **审批流** 引用。
- **报销**：员工费用申请，经 **审批流** 批准后进入财务流程。
- **公告 / 通知 / 待办**：消息触达三件套——公告全员广播、通知定向推送、待办关联审批任务。

### 培训与知识

- **培训中心**：知识库的独立入口（重构后归并；**考试模块已删除**）。
- **知识库**：企业内部文档/文章库，支持分类、阅读统计；附件支持**在线预览（KKFileView）**，无需下载即可查看 doc/xls/ppt/pdf 等格式。

## 领域模型（Domain Model）

> 基于以上术语与已确认业务规则构建（2026-08-12）。上下文落地到 NestJS modules 后可拆分为 CONTEXT-MAP.md + 各上下文 CONTEXT.md；当前先集中维护。

### 上下文地图（Bounded Contexts）

| 上下文 | 职责（是什么） | 关键实体 | 核心算法 |
|---|---|---|---|
| **组织与人员 Organization** | 员工档案、组织架构、身份与权限 | **员工**、**用户**、**部门**、**职位**、**角色**、权限点 | RBAC 判定；部门数据范围计算（C8） |
| **考勤 Attendance** | 打卡数据采集、考勤判定、排班、休假 | **打卡流水**、**班次**、**排班**、**考勤日报**、**考勤月报**、**请假**、**加班**、**休假额度**、补卡申请 | 打卡采集（XFace600 HTTP/CSV）；**考勤规则引擎**；额度换算 |
| **薪资 Payroll** | 工资计算与发放 | **薪资项目**、**算薪批次**、工资明细、**调整项**、**工资条** | **算薪引擎**；加班倍率；全勤判定 |
| **协同审批 Approval** | 审批链路与消息触达 | **审批流**、**审批组**、审批记录、公告、通知、待办 | 审批路由（按角色/审批组） |
| **财务 Expense** | 报销 | 报销单、报销明细 | 报销金额汇总 |
| **培训 Training** | 知识库（考试已删除） | 知识文章、分类、附件 | 文档预览（KKFileView 转码） |

### 上下文关系（依赖方向）

- **考勤 → 组织**：考勤依赖员工/部门（含数据隔离范围）
- **薪资 → 考勤 + 组织**：算薪输入 = **已确认的考勤月报快照** + 员工工资配置（不是实时打卡数据）
- **协同审批 ← 考勤 / 财务**：请假、补卡、报销发起审批
- **培训 → 组织**：仅依赖员工身份；报表中心是**展示层**，跨上下文只读，不持有领域数据

### 聚合根（Aggregate Roots）

| 聚合 | 包含 | 不变式（关键） |
|---|---|---|
| **员工** | 档案、部门/职位归属、基本工资、状态 | 工号唯一；离职后不可再排班 |
| **考勤日报** | 员工+日期、班次、上下班卡、判定结果 | 同一员工同一天唯一日报；补卡必须经审批 |
| **考勤月报** | 员工+月份、出勤/迟到/加班汇总 | 月报确认后锁定（算薪以确认版为准） |
| **算薪批次** | 月份、状态机、工资明细、调整项 | 月份唯一；状态机：草稿→确认→发布/撤回；撤回仅限未查看 |
| **休假额度** | 员工+休假类型、余额、变动记录 | 余额不可为负；调休 1:1 兑换 |

### 领域事件（解耦信号）

| 事件 | 触发 | 消费方动作 |
|---|---|---|
| 打卡已同步 PunchLogsSynced | 采集适配器写入流水后 | 触发当日考勤日报重算 |
| 月报已确认 AttendanceMonthlyConfirmed | 考勤月报确认时 | 触发算薪批次创建/重算 |
| 批次已发布 PayrollRunPublished | 算薪批次发布时 | 生成工资条 + 通知员工 |

### 算法边界（谁拥有算法，防止重复实现）

1. **打卡采集**（考勤）：XFace600 HTTP API / CSV → 只落原始流水，**不做业务解释**（迟到/早退由规则引擎判）。
2. **考勤规则引擎**（考勤）：流水 × 排班 → 日报/月报；是**唯一**产出考勤结果的算法。
3. **算薪引擎**（薪资）：输入已确认月报 + 请假/加班 + 薪资项目配置 → 明细 → 工资条；**只消费快照，可复算、可对账**。
4. **额度换算**（考勤）：调休 1:1、请假扣额度、加班兑换。
5. **审批路由**（协同）：按角色/审批组决定下一审批人。
6. **报表聚合**：考勤月报（考勤）、人力成本（薪资）——只读聚合，不修改领域数据。

### 术语待打磨（challenge）

- **"报表中心"与"考勤月报"**：报表是展示层聚合，月报是领域数据（已确认快照），两者不要混为一个概念——报表中心不持有数据。
- **"工资条"与"工资明细"**：明细是计算行（含调整项），工资条是发给员工的形式；展示 = 计算值 + 调整值。

## 关键约束

1. UI 必须遵守 Arco Design Pro 企业级标准（REFACTOR_PLAN.md 第 11 章，强制）。
2. 重复组件必须抽取公共组件统一调用（第 11.5 节）。
3. 数据访问一律走 Prisma；金额用 decimal，禁止浮点。
4. 删除功能必须连根删：前端页面 + 后端路由 + 数据表 + 依赖。
5. 已确认删除：聊天、质检、资产/库存/设备、案例库；公告保留。
6. 业务规则以本文档"业务规则"节 + `docs/pending-decisions.md` 为准，变更需先更新文档再改代码。

## 文档职责边界（防止重复维护）

- **CONTEXT.md = 领域上下文（是什么）**：术语、已确认业务规则、约束、文档地图。任何会话/任何任务的起点。
- **REFACTOR_PLAN.md = 行动方案（怎么做）**：技术栈、功能去留、路线图、UI 规范。
- **ADR = 决策与理由（为什么）**：已拍板决策的历史记录（当前 10 个）。
- **tickets = 任务清单（做什么）**：阶段化执行清单。
- **schema / api = 技术细节契约**：表字段、接口出入参、错误码。
- **铁律：各文档只在自己职责内维护，不互相复制内容，只互相引用。** 内容变更先改源头文档；CONTEXT.md 只保留摘要与指向，不搬 REFACTOR_PLAN.md 等文档的正文。

## 文档地图

| 文档 | 位置 | 用途 |
|---|---|---|
| 重构方案（权威） | `REFACTOR_PLAN.md` | 技术栈、去留、路线图、UI 规范 |
| **整体架构设计** | `docs/architecture.md` | 分层架构、模块拆分、依赖规则、Prisma 组织、前端 features |
| **技术规格说明书** | `docs/spec/technical-spec.md` | **系统目标态全貌**：业务/算法/Schema/接口/前端/非功能 |
| 决策记录（已确认） | `docs/pending-decisions.md` | 质询产出的 24 项决策，全部确认 |
| 架构决策 | `docs/adr/` | ADR-0001~0010（技术栈/纯Web/RDS/workspace/删除边界/UI规范/Refine×Arco/数据迁移/上传/数据隔离） |
| 新增表字段 | `docs/schema/new-tables.md` | 7 张新表字段草案 v0.1 |
| API 契约 + 错误码 | `docs/api/core-contracts.md` | REST 契约、错误码、校验规则、认证细节 |
| 任务拆解 | `docs/tickets/` | 可执行的 ticket 清单 |
| Issue Tracker 配置 | `docs/agents/issue-tracker.md` | ticket 的发布方式 |
| 旧代码归档 | `legacy/` | 旧版源码快照，仅备查 |
