# 🚀 雷犀客服管理系统 (LeiXi System) 核心优化方案 (完整版)

本方案旨在通过现代化的技术栈重构（TypeScript + Mantine + Prisma + React Query），解决当前系统存在的 `useEffect` 乱象、组件碎片化、数据库维护困难及响应延迟问题，打造一个具备原生桌面质感的高性能企业级应用。

---

## 一、 核心技术栈选定 (The "Elite" Stack)

| 维度            | 推荐选型                    | 核心优势                                                          |
|:------------- |:----------------------- |:------------------------------------------------------------- |
| **编程语言**      | **TypeScript (TS)**     | **必须引入**。提供全链路类型检查，将 Bug 消灭在编译阶段，配合 Prisma 效果翻倍               |
| **UI 组件库**    | **Mantine v7**          | 高信息密度、专业桌面质感、内置 100+ 强大 Hooks，逻辑极其严密                          |
| **ORM (数据库)** | **Prisma**              | 类型安全、全代码补全、极速开发体验、关系处理简单                                      |
| **数据同步**      | **TanStack Query (v5)** | 全局缓存、自动静默刷新、彻底消灭 `useEffect` 加载逻辑                             |
| **路由系统**      | **React Router v6.4+**  | 利用 Data APIs (Loaders/Actions) 实现“边取边刷” (Render-as-you-fetch) |
| **校验工具**      | **Zod**                 | 与 Prisma 和 Mantine 表单完美集成，实现端到端的类型安全                          |

---

## 二、 前端架构优化：消灭冗余与提升质感

### 1. 彻底消灭 `useEffect` 数据加载

不再在组件挂载时通过 `useEffect` 抓取数据，转而使用 **React Router Loader + React Query** 预取机制。

* **流程**：用户点击跳转 -> Router Loader 触发请求 -> 数据存入 Query 缓存 -> 页面挂载瞬间从缓存读取。
* **价值**：消除页面跳转后的“白屏”或“转圈”感，实现瞬发渲染。

### 2. 建立统一 UI 组件库 (Shared UI Kit)

针对“组件碎片化”问题，在 `src/components/common` 基于 Mantine 进行二次封装：

* **LXTable**: 集成搜索、分页、排序的标准化表格。
* **LXSelect / LXPaginator**: 统一样式与交互逻辑。
* **视觉风格**: 调低圆角（`radius: "sm"`)，增加边框质感，通过增加布局密度营造“专业办公软件”印象。

---

## 三、 工程化标准化 (Standardization)

### 1. 代码规范 (Linter & Formatter)

* **ESLint**: 引入 `eslint-plugin-react-hooks` 和 `@typescript-eslint/recommended`，严格限制 `any` 的使用。
* **Prettier**: 强制执行统一的代码格式化规范（分号、单引号、2空格缩进）。
* **TS 配置**: 开启 `strict: true`，确保代码质量。

### 2. 代码守卫 (Git Hooks)

* **Husky + Lint-staged**: 在每次 `git commit` 前自动执行代码扫描和格式化，禁止不符合规范的代码入库。
* **Commitlint**: 强制执行 Angular 规范的 Commit Message (如 `feat:`, `fix:`, `docs:`)。

---

## 四、 后端与数据库优化：类型安全与极致性能

### 1. Prisma ORM 接入

* **核心逻辑**: 弃用明文 SQL 字符串拼接，利用 Prisma 的 `Introspection` 自动从数据库提取 Schema。
* **优势**: 获得从数据库到 API 层面的全链路类型推导，彻底杜绝字段名拼错。

### 2. Fastify Schema 序列化

* **优化**: 为每个 API 路由定义严谨的 JSON Schema（由 Zod 或 TS 类型生成）。
* **性能**: Fastify 会预编译这些 Schema，使序列化速度提升 3-5 倍。

---

## 五、 实时性与缓存层优化 (Redis & WebSocket)

### 1. WebSocket 工业级改造

* **序列化**: 引入 `socket.io-msgpack-parser`。
* **状态管理**: 收到消息后直接调用 `queryClient.setQueryData` 手动更新缓存，实现 UI 零延迟同步。

### 2. Redis 深度优化

* **哈希存储 (Hash)**：用户信息等复杂对象使用 `HSET`，避免频繁的 JSON 解析。
* **集合校验 (Set)**：权限校验使用 `SISMEMBER`，秒级返回结果。

---

## 六、 推荐的目录结构 (Feature-based)

```text
src/
├── core/                 # 全局基础能力 (Theme, Axios, QueryClient)
├── components/           # 全局公用 LX 系列组件
├── features/             # ★ 业务核心模块 ★
│   ├── assets/           # 资产管理模块
│   │   ├── api/          # 接口定义 (React Query Hooks)
│   │   ├── components/   # 模块专用组件
│   │   ├── hooks/        # 模块业务逻辑
│   │   ├── types.ts      # 模块类型定义
│   │   └── index.tsx     # 模块导出入口
│   └── chat/             # 聊天系统
└── pages/                # 页面容器（仅负责路由组合）
```
