# 权限管理使用示例

本文档提供权限管理系统的实际使用示例，包括前端和后端的代码示例。

## 📋 目录

1. [前端使用示例](#前端使用示例)
2. [后端使用示例](#后端使用示例)
3. [完整功能示例](#完整功能示例)

---

## 前端使用示例

### 1. 登录后加载用户权限

```javascript
// Login.jsx
import { loadUserPermissions } from '../utils/permission'

const handleLogin = async (username, password) => {
  try {
    const response = await fetch('http://localhost:3001/api/auth/login', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ username, password })
    })

    const data = await response.json()

    if (data.success) {
      // 保存用户信息和token
      localStorage.setItem('token', data.token)
      localStorage.setItem('user', JSON.stringify(data.user))

      // 加载用户权限
      await loadUserPermissions(data.user.id)

      // 跳转到首页
      navigate('/')
    }
  } catch (error) {
    console.error('登录失败:', error)
  }
}
```

### 2. 条件渲染按钮

```javascript
// EmployeeManagement.jsx
import { hasPermission, PERMISSIONS } from '../utils/permission'

function EmployeeManagement() {
  return (
    <div>
      <h2>员工管理</h2>

      {/* 只有拥有创建权限的用户才能看到添加按钮 */}
      {hasPermission(PERMISSIONS.EMPLOYEE_CREATE) && (
        <button onClick={handleCreate}>添加员工</button>
      )}

      <table>
        <tbody>
          {employees.map(emp => (
            <tr key={emp.id}>
              <td>{emp.name}</td>
              <td>
                {/* 编辑按钮 */}
                {hasPermission(PERMISSIONS.EMPLOYEE_UPDATE) && (
                  <button onClick={() => handleEdit(emp)}>编辑</button>
                )}

                {/* 删除按钮 */}
                {hasPermission(PERMISSIONS.EMPLOYEE_DELETE) && (
                  <button onClick={() => handleDelete(emp.id)}>删除</button>
                )}
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  )
}
```

### 3. 使用权限容器组件

```javascript
// Dashboard.jsx
import { PermissionContainer, PERMISSIONS } from '../utils/permission'

function Dashboard() {
  return (
    <div>
      <h1>仪表盘</h1>

      {/* 只有管理员才能看到的统计卡片 */}
      <PermissionContainer
        permissions={[PERMISSIONS.USER_LIST, PERMISSIONS.ROLE_LIST]}
        mode="any"
      >
        <div className="admin-stats">
          <h3>管理员统计</h3>
          <p>用户总数: {userCount}</p>
          <p>角色总数: {roleCount}</p>
        </div>
      </PermissionContainer>
员才能看到的质检统计 */}
      <PermissionContainer permission={PERMISSIONS.QUALITY_STATISTICS}>
        <div className="quality-stats">
          <h3>质检统计</h3>
          <p>今日质检: {todayQuality}</p>
        </div>
      </PermissionContainer>

      {/* 无权限时显示提示 */}
      <PermissionContainer
        permission={PERMISSIONS.SYSTEM_CONFIG}
        fallback={<p>您没有权限查看系统配置</p>}
      >
        <div className="system-config">
          <h3>系统配置</h3>
          {/* 配置内容 */}
        </div>
      </PermissionContainer>
    </div>
  )
}
```

### 4. 使用权限按钮组件

```javascript
// Toolbar.jsx
import { PermissionButton, PERMISSIONS } from '../utils/permission'

function Toolbar() {
  return (
    <div className="toolbar">
      {/* 只有拥有权限才显示的按钮 */}
      <PermissionButton
        permission={PERMISSIONS.EMPLOYEE_CREATE}
        onClick={handleCreate}
        className="btn-primary"
      >
        添加员工
      </PermissionButton>

      <PermissionButton
        permission={PERMISSIONS.DEPARTMENT_CREATE}
        onClick={handleCreateDept}
        className="btn-secondary"
      >
        添加部门
      </PermissionButton>

      <PermissionButton
        permission={PERMISSIONS.SYSTEM_BACKUP}
        onClick={handleBackup}
        className="btn-danger"
      >
        数据备份
      </PermissionButton>
    </div>
  )
}
```

### 5. 路由权限守卫

```javascript
// App.jsx
import { useEffect } from 'react'
import { useNavigate } from 'react-router-dom'
import { canAccessRoute, PERMISSIONS } from './utils/permission'

function ProtectedRoute({ children, permissions, mode = 'any' }) {
  const navigate = useNavigate()

  useEffect(() => {
    const hasAccess = canAccessRoute(permissions, mode, null)
    if (!hasAccess) {
      navigate('/unauthorized')
    }
  }, [permissions, mode, navigate])

  return children
}

// 使用示例
function App() {
  return (
    <Routes>
      <Route path="/login" element={<Login />} />

      {/* 需要员工管理权限才能访问 */}
      <Route
        path="/employees"
        element={
          <ProtectedRoute permissions={PERMISSIONS.EMPLOYEE_LIST}>
            <EmployeeManagement />
          </ProtectedRoute>
        }
      />

      {/* 需要系统管理员权限才能访问 */}
      <Route
        path="/permissions"
        element={
          <ProtectedRoute
            permissions={[PERMISSIONS.ROLE_LIST, PERMISSIONS.USER_ASSIGN_ROLE]}
            mode="all"
          >
            <PermissionManagement />
          </ProtectedRoute>
        }
      />

      <Route path="/unauthorized" element={<Unauthorized />} />
    </Routes>
  )
}
```

### 6. 检查多个权限

```javascript
// QualityInspection.jsx
import { hasAnyPermission, hasAllPermissions, PERMISSIONS } from '../utils/permission'

function QualityInspection() {
  // 检查是否有质检相关的任一权限
  const canViewQuality = hasAnyPermission([
    PERMISSIONS.QUALITY_LIST,
    PERMISSIONS.QUALITY_INSPECT,
    PERMISSIONS.QUALITY_STATISTICS
  ])

  // 检查是否同时拥有创建和执行质检的权限
  const canManageQuality = hasAllPermissions([
    PERMISSIONS.QUALITY_CREATE,
    PERMISSIONS.QUALITY_INSPECT
  ])

  if (!canViewQuality) {
    return <div>您没有权限访问质检模块</div>
  }

  return (
    <div>
      <h2>质检管理</h2>

      {canManageQuality && (
        <button onClick={handleCreateQuality}>创建质检任务</button>
      )}

      {/* 其他内容 */}
    </div>
  )
}
```

---

## 后端使用示例

### 1. 引入权限中间件

```javascript
// server/index.js
const { checkPermission, checkRoleLevel } = require('./middleware/permission')

// 在路由中使用权限检查
fastify.post('/api/employees', {
  preHandler: [
    verifyToken,  // 先验证登录
    checkPermission(pool, 'employee:create')  // 再检查权限
  ]
}, async (request, reply) => {
  // 创建员工的逻辑
  const { employee_no, real_name, email } = request.body
  // ...
})
```

### 2. 检查多个权限（OR关系）

```javascript
// 用户只需要有其中一个权限即可
fastify.get('/api/quality-sessions', {
  preHandler: [
    verifyToken,
    checkPermission(pool, ['quality:list', 'quality:inspect'], 'any')
  ]
}, async (request, reply) => {
  // 获取质检会话列表
})
```

### 3. 检查多个权限（AND关系）

```javascript
// 用户必须同时拥有所有权限
fastify.post('/api/roles/:id/permissions', {
  preHandler: [
    verifyToken,
    checkPermission(pool, ['role:update', 'role:assign_permission'], 'all')
  ]
}, async (request, reply) => {
  // 为角色分配权限
})
```

### 4. 检查角色级别

```javascript
// 只有角色级别>=7的用户才能访问
fastify.get('/api/admin/statistics', {
  preHandler: [
    verifyToken,
    checkRoleLevel(pool, 7)
  ]
}, async (request, reply) => {
  // 管理员统计数据
})
```

### 5. 在路由处理函数中手动检查权限

```javascript
const { hasPermission } = require('./middleware/permission')

fastify.put('/api/employees/:id', {
  preHandler: [verifyToken]
}, async (request, reply) => {
  const { id } = request.params
  const userId = request.user.id

  // 检查是否有编辑权限
  const canEdit = await hasPermission(pool, userId, 'employee:update')

  if (!canEdit) {
    return reply.code(403).send({
      success: false,
      message: '没有权限编辑员工信息'
    })
  }

  // 执行更新逻辑
  // ...
})
```

### 6. 根据权限返回不同数据

```javascript
const { getUserPermissions } = require('./middleware/permission')

fastify.get('/api/employees', {
  preHandler: [verifyToken]
}, async (request, reply) => {
  const userId = request.user.id

  // 获取用户的所有权限
  const permissions = await getUserPermissions(pool, userId)

  // 基础查询
  let query = 'SELECT * FROM employees'

  // 如果没有查看所有员工的权限，只返回自己的信息
  if (!permissions.includes('employee:list')) {
    query += ' WHERE user_id = ?'
    const [rows] = await pool.query(query, [userId])
    return rows
  }

  // 有权限，返回所有员工
  const [rows] = await pool.query(query)

  // 如果没有查看薪资权限，隐藏薪资字段
  if (!permissions.includes('employee:view_salary')) {
    rows.forEach(emp => {
      delete emp.salary
    })
  }

  return rows
})
```

### 7. 权限检查装饰器模式

```javascript
// 创建一个权限检查装饰器
function requirePermission(permission) {
  return {
    preHandler: [
      verifyToken,
      checkPermission(pool, permission)
    ]
  }
}

// 使用装饰器
fastify.post('/api/departments',
  requirePermission('department:create'),
  async (request, reply) => {
    // 创建部门逻辑
  }
)

fastify.put('/api/departments/:id',
  requirePermission('department:update'),
  async (request, reply) => {
    // 更新部门逻辑
  }
)

fastify.delete('/api/departments/:id',
  requirePermission('department:delete'),
  async (request, reply) => {
    // 删除部门逻辑
  }
)
```

---

## 完整功能示例

### 示例1: 员工管理页面（完整）

```javascript
// EmployeeManagement.jsx
import React, { useState, useEffect } from 'react'
import { hasPermission, PERMISSIONS, PermissionContainer } from '../utils/permission'
import { toast } from 'react-toastify'

function EmployeeManagement() {
  const [employees, setEmployees] = useState([])
  const [loading, setLoading] = useState(true)

  // 权限检查
  const canCreate = hasPermission(PERMISSIONS.EMPLOYEE_CREATE)
  const canUpdate = hasPermission(PERMISSIONS.EMPLOYEE_UPDATE)
  const canDelete = hasPermission(PERMISSIONS.EMPLOYEE_DELETE)
  const canViewDetail = hasPermission(PERMISSIONS.EMPLOYEE_DETAIL)

  useEffect(() => {
    fetchEmployees()
  }, [])

  const fetchEmployees = async () => {
    try {
      const response = await fetch('http://localhost:3001/api/employees', {
        headers: {
          'Authorization': `Bearer ${localStorage.getItem('token')}`
        }
      })
      const data = await response.json()
      setEmployees(data)
    } catch (error) {
      toast.error('获取员工列表失败')
    } finally {
      setLoading(false)
    }
  }

  const handleCreate = () => {
    if (!canCreate) {
      toast.error('没有权限创建员工')
      return
    }
    // 打开创建对话框
  }

  const handleEdit = (employee) => {
    if (!canUpdate) {
      toast.error('没有权限编辑员工')
      return
    }
    // 打开编辑对话框
  }

  const handleDelete = async (id) => {
    if (!canDelete) {
      toast.error('没有权限删除员工')
      return
    }

    if (!confirm('确定要删除这个员工吗？')) return

    try {
      const response = await fetch(`http://localhost:3001/api/employees/${id}`, {
        method: 'DELETE',
        headers: {
          'Authorization': `Bearer ${localStorage.getItem('token')}`
        }
      })

      if (response.ok) {
        toast.success('删除成功')
        fetchEmployees()
      }
    } catch (error) {
      toast.error('删除失败')
    }
  }

  const handleViewDetail = (employee) => {
    if (!canViewDetail) {
      toast.error('没有权限查看员工详情')
      return
    }
    // 打开详情对话框
  }

  if (loading) {
    return <div>加载中...</div>
  }

  return (
    <div className="employee-management">
      <div className="header">
        <h2>员工管理</h2>

        {/* 只有有创建权限的用户才能看到添加按钮 */}
        <PermissionContainer permission={PERMISSIONS.EMPLOYEE_CREATE}>
          <button onClick={handleCreate} className="btn-primary">
            添加员工
          </button>
        </PermissionContainer>
      </div>

      <table className="employee-table">
        <thead>
          <tr>
            <th>工号</th>
            <th>姓名</th>
            <th>部门</th>
            <th>职位</th>
            <th>状态</th>
            <th>操作</th>
          </tr>
        </thead>
        <tbody>
          {employees.map(emp => (
            <tr key={emp.id}>
              <td>{emp.employee_no}</td>
              <td>{emp.real_name}</td>
              <td>{emp.department_name}</td>
              <td>{emp.position}</td>
              <td>{emp.status}</td>
              <td>
                {/* 查看详情 */}
                <PermissionContainer permission={PERMISSIONS.EMPLOYEE_DETAIL}>
                  <button onClick={() => handleViewDetail(emp)}>
                    详情
                  </button>
                </PermissionContainer>

                {/* 编辑 */}
                <PermissionContainer permission={PERMISSIONS.EMPLOYEE_UPDATE}>
                  <button onClick={() => handleEdit(emp)}>
                    编辑
                  </button>
                </PermissionContainer>

                {/* 删除 */}
                <PermissionContainer permission={PERMISSIONS.EMPLOYEE_DELETE}>
                  <button onClick={() => handleDelete(emp.id)}>
                    删除
                  </button>
                </PermissionContainer>
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  )
}

export default EmployeeManagement
```

### 示例2: 员工管理API（完整）

```javascript
// server/routes/employees.js
const { checkPermission } = require('../middleware/permission')

module.exports = async function (fastify, opts) {
  const { pool } = opts

  // 获取员工列表
  fastify.get('/api/employees', {
    preHandler: [
      verifyToken,
      checkPermission(pool, 'employee:list')
    ]
  }, async (request, reply) => {
    try {
      const [rows] = await pool.query(`
        SELECT
          e.*,
          u.real_name,
          u.email,
          u.phone,
          d.name as department_name
        FROM employees e
        LEFT JOIN users u ON e.user_id = u.id
        LEFT JOIN departments d ON u.department_id = d.id
        ORDER BY e.created_at DESC
      `)
      return rows
    } catch (error) {
      console.error(error)
      reply.code(500).send({ error: '获取员工列表失败' })
    }
  })

  // 创建员工
  fastify.post('/api/employees', {
    preHandler: [
      verifyToken,
      checkPermission(pool, 'employee:create')
    ]
  }, async (request, reply) => {
    const { employee_no, real_name, email, phone, department_id, position } = request.body

    try {
      // 创建用户
      const passwordHash = await bcrypt.hash('123456', 10)
      const [userResult] = await pool.query(
        'INSERT INTO users (username, password_hash, real_name, email, phone, department_id) VALUES (?, ?, ?, ?, ?, ?)',
        [employee_no, passwordHash, real_name, email, phone, department_id]
      )

      // 创建员工
      await pool.query(
        'INSERT INTO employees (user_id, employee_no, position, hire_date) VALUES (?, ?, ?, NOW())',
        [userResult.insertId, employee_no, position]
      )

      return { success: true, id: userResult.insertId }
    } catch (error) {
      console.error(error)
      reply.code(500).send({ error: '创建员工失败' })
    }
  })

  // 更新员工
  fastify.put('/api/employees/:id', {
    preHandler: [
      verifyToken,
      checkPermission(pool, 'employee:update')
    ]
  }, async (request, reply) => {
    const { id } = request.params
    const { real_name, email, phone, department_id, position } = request.body

    try {
      // 更新用户信息
      await pool.query(
        'UPDATE users SET real_name = ?, email = ?, phone = ?, department_id = ? WHERE id = (SELECT user_id FROM employees WHERE id = ?)',
        [real_name, email, phone, department_id, id]
      )

      // 更新员工信息
      await pool.query(
        'UPDATE employees SET position = ? WHERE id = ?',
        [position, id]
      )

      return { success: true }
    } catch (error) {
      console.error(error)
      reply.code(500).send({ error: '更新员工失败' })
    }
  })

  // 删除员工
  fastify.delete('/api/employees/:id', {
    preHandler: [
      verifyToken,
      checkPermission(pool, 'employee:delete')
    ]
  }, async (request, reply) => {
    const { id } = request.params

    try {
      await pool.query('DELETE FROM employees WHERE id = ?', [id])
      return { success: true }
    } catch (error) {
      console.error(error)
      reply.code(500).send({ error: '删除员工失败' })
    }
  })

  // 获取员工详情
  fastify.get('/api/employees/:id', {
    preHandler: [
      verifyToken,
      checkPermission(pool, 'employee:detail')
    ]
  }, async (request, reply) => {
    const { id } = request.params

    try {
      const [rows] = await pool.query(`
        SELECT
          e.*,
          u.real_name,
          u.email,
          u.phone,
          u.avatar,
          d.name as department_name
        FROM employees e
        LEFT JOIN users u ON e.user_id = u.id
        LEFT JOIN departments d ON u.department_id = d.id
        WHERE e.id = ?
      `, [id])

      if (rows.length === 0) {
        return reply.code(404).send({ error: '员工不存在' })
      }

      return rows[0]
    } catch (error) {
      console.error(error)
      reply.code(500).send({ error: '获取员工详情失败' })
    }
  })
}
```

---

## 总结

通过以上示例，你可以：

1. ✅ 在前端实现细粒度的权限控制
2. ✅ 在后端保护API接口安全
3. ✅ 灵活组合多个权限检查
4. ✅ 根据权限动态显示/隐藏功能
5. ✅ 提供良好的用户体验

记住：**前后端都要做权限检查**，前端控制显示，后端保证安全！
