# S04 · 班次与排班

## What to build
考勤基础配置：班次维护（含跨天标志 isNextDay，C1）+ 排班分配（员工×日期×班次，月历视图 + 批量排班），排班数据可查可导。

## 五维清单
- **数据库**：shifts/schedules（uk(employee_id, work_date) 排班唯一）
- **后端接口**：GET/POST /shifts、GET/POST /schedules、POST /schedules/batch；错误码 2001（班次冲突）/2002（排班重复）
- **业务算法**：跨天班次时间校验（isNextDay）；批量排班冲突检测（事务原子回滚）
- **前端页面**：attendance feature 班次管理页 + 排班月历视图（D2，批量排班；拖拽留后续迭代）
- **单元测试**：API e2e 11 项（班次 CRUD/重复排班 2002/批量排班/权限隔离）

## Acceptance criteria
- [x] 班次支持跨天（22:00-06:00）配置
- [x] 月历视图显示员工排班，可批量排班
- [x] 重复排班被拒绝（2002）

## Blocked by
- S03 员工档案 ✅

---

## 进度 · TDD 后端实现（已完成）

### 完成项
- [x] **班次管理** `GET/POST /shifts`
  - 创建班次：名称唯一、跨天标志校验（C1）
  - 班次列表：全量返回
  - 错误码：2001（时间无效/名称重复）
- [x] **排班管理** `GET/POST /schedules`
  - 创建排班：uk(employeeId, workDate) 保证唯一
  - 排班列表：按员工/日期范围筛选，分页
  - 数据隔离：经理只能给本部门员工排班，只能看本部门
- [x] **批量排班** `POST /schedules/batch`
  - 事务原子性：任一重复则全部回滚
  - 错误码：2002（重复排班）
- [x] **权限控制**：attendance:manage 可管理，attendance:view 可查看
- [x] **e2e 测试**：11 项全部 PASS

### 测试数据
- e2e 测试：`tests/shifts-schedules.e2e.test.ts`（11 e2e）
- 覆盖场景：班次创建/跨天班次/时间校验/班次列表/无权限/排班创建/重复排班/批量排班/批量回滚/筛选/不存在员工

### 未实现（后续切片）
- [x] 班次编辑/删除（PUT/DELETE）
- [x] 排班编辑/删除（PUT/DELETE）
- [ ] 前端排班月历视图（拖拽调整）
- [ ] 排班导入（Excel）
- [ ] 轮班规则（周期性排班模板）

---

## 迭代二 · 班次/排班编辑删除（TDD 完成）

### 完成项
- [x] **班次编辑** `PUT /shifts/:id`
  - 支持部分字段更新（名称/时间/跨天标志）
  - 保留跨天校验逻辑
  - 名称唯一约束
- [x] **班次删除** `DELETE /shifts/:id`
  - 被排班使用的班次不可删除（业务校验）
  - 不存在返回 404
- [x] **排班编辑** `PUT /schedules/:id`
  - 支持更换班次、调整日期、调换员工
  - 员工×日期唯一约束冲突检测
  - 数据隔离校验
- [x] **排班删除** `DELETE /schedules/:id`
  - 数据隔离：只能删除可见范围内员工的排班

### 测试数据
- e2e 测试：`tests/shift-schedule-edit.e2e.test.ts`（14 e2e）
- 覆盖场景：班次更新/部分更新/跨天校验/404/班次删除/使用中不可删/排班换班/改日期/冲突检测/404/排班删除/404
