# 雷犀旗舰版 系统优化记录流水 (V2)

## [2026-03-08] 智能调度审计重构与知识库 Win11 旗舰化升级

### 1. 考勤排班中心 (V5 审计引擎)
- **调度逻辑重塑**：将“智能调度”升级为“排班统计与审计”中心。支持从宏观（部门覆盖率）到微观（个人出勤分布）的全方位合规性校验。
- **全员覆盖率审计**：新增左侧审计面板，支持多选班次范围（包含全公司通用班次）。系统自动计算每日班次覆盖缺口，并辅以高亮红色日历块与呼吸灯警示 Banner，确保“不漏岗、不空岗”。
- **个人出勤分布 (可视化)**：引入“堆叠分布条 (Stacked Bar)”设计。不同班次的出勤占比以多色比例条形式直观展示，解决了多班次场景下列表拥挤的问题。
- **排班统计自动化**：自动汇总每位职员的月休天数、已审批请假天数及详细的出勤班次计数。
- **导出链路修复**：
    - 重构了 `server/routes/schedule-excel.js`，在 Excel 排班表末尾自动追加个人统计列。
    - 修复了部门考勤表按日期范围导出的 500 错误，后端已支持 `start_date` / `end_date` 精准过滤。
- **UI 质感升级**：统计弹窗宽度扩展至 1000px，全量执行表头内容垂直水平居中，优化了高对比度文本色彩。

### 2. 知识库系统 (Win11 风格全量对齐)
- **架构版本迁移**：在 `App.jsx` 中彻底移除旧版 `KnowledgeBase` 与 `MyKnowledgeBase`，全量切换为基于 `Win11KnowledgeFolderView` 的旗舰组件。
- **数据源严格隔离**：通过 `viewMode` (public/personal/management) 机制，彻底修复了公共知识库与个人库数据混淆的问题。API 调用现在严格区分，确保公共文档与个人收藏互不干扰。
- **图标轻量化重绘**：全面引入 `lucide-react` 图标库，将沉重的 Emoji 和实心图标替换为扁平化的细线描边图标。
- **自愈式渲染助手**：新增了 `renderArticleIcon` 智能映射函数。在不改动数据库 Emoji 结构的前提下，前端自动将其转换为现代感极强的 Lucide 矢量组件。
- **工程化清理**：物理删除了已废弃的旧版代码文件，消除了后续维护的冗余风险。

## [2026-03-07] 全量架构与交互优化

### 1. 核心链路重构
- **日志系统分级**：引入了统一的 `logger` 工具，配合 `DEBUG_MODE` 开关。在生产环境下自动屏蔽 `debug` 和 `info` 日志，彻底消除了控制台刷屏问题。
- **API 客户端规范化**：全量清理了 `axios` 原始调用，统一改用封装好的 `api` 实例。解决了 5173 端口请求错误、Token 自动注入失败等 404/500 问题。
- **自愈拦截器**：增强了 API 拦截器，自动识别并修复重复的 `/api` 前缀及环境识别逻辑。

### 2. 考勤中心升级 (V4 引擎)
- **考勤数据驾驶舱**：将部门报表重构为“驾驶舱模式”，支持部门一键切换、大数字看板及加班 Top 5 统计。
- **每日明细流水**：实现了详情页全月日历补全。即便某天没打卡没排班，也会显示“未排班/休息”，确保数据流水逻辑连贯不留白。
- **全量居中美学**：表格所有表头及内容均执行了严格的水平垂直居中对齐，采用 11px 专业微缩字体。

### 3. 即时通讯与交互优化
- **通讯实时性修复**：解决了消息发送后右侧窗口不刷新的 Bug，并彻底根除了因“陈旧闭包”导致的消息发错群组问题。
- **消息摘要增强**：侧边栏联系人列表能精准识别并显示 `[图片]`、`[文件]` 等非文本摘要。
- **标签页持久化**：实现了 `App.jsx` 状态存储。系统会自动记忆最后一次打开的页面，刷新后 精准恢复，不再重置回仪表盘。

### 4. 系统管理与安全性
- **DB 变更守护 (Skill)**：建立了 `db-migration-guard` 流程，所有数据库修改必须同步更新 `update.sql` 物理日志。
- **RBAC 自动同步 (Skill)**：新增功能权限（如考勤看板查看）已实现“数据库自动录入 + 超管 授权”闭环。
- **侧边栏自动导航**：刷新页面后，侧边栏会自动计算路径并展开对应的一级/二级菜单。
