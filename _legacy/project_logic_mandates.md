# 雷犀系统核心逻辑与视觉准则 (Logic & UI Mandates)

## 1. 视觉标准 (Visual Standards)
- **边框标准**: 所有交互控件（Input, Select, DatePicker, Button）统一使用 **1px** 边框，颜色锁定为 **slate-500 (#64748b)**。
- **圆角规范**: 容器圆角 **16px**，内部控件圆角 **8px**。
- **字体标准**: 姓名/主标题 **15px**，辅助信息 **13px**，全量使用 **font-black (900)** 粗体，严禁使用斜体。
- **高度标准**: 交互控件高度统一锁定为 **h-11 (44px)**。
- **圆角修复**: 严禁在 Select 组件上使用内联 style 强制 border（会导致圆角断裂），必须通过顶层 `ConfigProvider.theme.token.colorBorder` 统筹。

## 2. 搜索布局标准 (Search Integrated Standard) - [NEW]
- **单行全铺满**: 搜索筛选区域必须采用 `flex` 布局并占满整行宽度（`w-full`）。
- **自适应比例**: 
    - 文本检索 (Search Input): 建议 `flex-[1.5]` 或 `min-w-[180px]`。
    - 下拉筛选 (Select): 建议 `flex-1` 或 `min-w-[140px]`。
    - 日期范围 (RangePicker): 建议 `flex-[2.5]` 或 `min-w-[280px]`。
- **超集成快捷条**: “今天、近7天、本月累计”等快捷按钮必须与搜索条件置于同一水平行，或通过物理缝合形成连续的交互流水线。
- **即时联动**: 快捷按钮点击必须瞬间触发 API 请求，实现零延迟反馈。

## 3. 分页标准 (Pagination Standard)
- **物理植入**: 表格下方必须具备标准化分页器区块。
- **功能矩阵**: 
    - 展示“共 X 条”审计明细。
    - 包含“单页条数切换” (10, 20, 50)。
    - 页码联动按钮（选中态为 Slate-900）。
    - 具备“跳至 X 页”输入框及 ArrowRight 触发按钮。
- **显示策略**: 仅在记录数 > 10 时显示分页器。

## 4. 白话化标准 (Colloquial Standard)
- **术语去生癖**: 使用“执行者”、“业务域”、“行为明细”、“激活/注销”、“名册”、“下载”等直白术语。
- **禁止英文**: 面向中文用户，所有 Label 和 Tooltip 必须提供精准的中文化描述。

## 5. 安全与逻辑红线
- **职位保护**: 职位下员工数 > 0 时，物理拦截“删除/停用”操作并弹出 toast.error。
- **资产联动**: 员工离职/注销时，必须先通过 `isAssetConfirmModalOpen` 校验并物理回收在用设备。
- **状态同步**: 修改当前登录用户资料后，必须触发 `window.dispatchEvent(new Event('storage'))` 同步全局缓存。
