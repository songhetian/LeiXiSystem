# prototypes/ — 原型验证区（throwaway，非生产代码）

> 用途：回答两类验证问题。**验证完删除，结论已捕获到 NOTES。**
> 对应技能：prototype（LOGIC + UI 分支）

## 1. 算法原型 — `engine-demo.mjs`（已运行 ✅）

- **验证问题**：考勤规则引擎 + 算薪引擎输出是否匹配 `docs/spec/technical-spec.md` 第 2 章预期。
- **运行**：`node engine-demo.mjs`
- **结果**：23/23 断言 PASS（考勤 11 项、算薪 12 项，含 3 名员工对账样例）。
- **NOTES（验证结论 + 暴露的 spec 缺口，需决策）**：
  1. **加班费小时基数未定义**：原型采用 `基本工资 ÷ 21.75 ÷ 8` → ✅ **2026-08-12 已确认**（标准月计薪天数 21.75）。
  2. **请假与缺勤口径**：请假 3 天按 absentDays 计扣款 → ✅ **2026-08-12 已确认**（请假计入缺勤扣款，统一口径）。
  3. **跨天班次加班边界**：按"班次结束时间"判定（06:10 → 加班 10 分钟）→ ✅ **2026-08-12 已确认**。
  - 三项已回写 `docs/spec/technical-spec.md`、CONTEXT.md、REFACTOR_PLAN.md。

## 2. 交互原型 — `ui-payroll.html`（浏览器打开即可）

- **验证问题**：算薪试算 → 抽检确认 → 发布 操作流程是否顺畅（spec 5.3 / ADR-0011 / D1）。
- **运行**：浏览器直接打开 `ui-payroll.html`。
- **覆盖交互**：① 试算表逐员工明细 + 展开查看 + 差异高亮（>5%）② 强制抽检 3 人（勾满才可确认，ADR-0011 抽检）③ 确认/发布/撤回状态流转（草稿→已确认→已发布）④ 导出按钮（占位）。
- **NOTES（交互验证结论）**：
  - 三阶段步骤条 + 状态 tag 清晰，抽检强制勾 3 人机制符合 spec D1；
  - 行内展开明细比弹窗更顺（建议生产用行内展开或 Drawer）；
  - 差异高亮（>5%）在试算阶段提供，生产可加"高亮原因 tooltip"。

## 原型 → 生产的承接

- 引擎逻辑验证通过后，移植为 `apps/server/src/modules/{attendance,payroll}/domain/` 纯函数（架构文档第 2 章），原型文件删除。
- 交互验证通过后，按 Arco Pro 规范重写为生产页面（REFACTOR_PLAN 第 11 章），本 HTML 删除。
- spec 缺口 3 项确认后回写 `docs/spec/technical-spec.md` 与 CONTEXT.md。
