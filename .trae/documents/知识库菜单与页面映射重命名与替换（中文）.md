## 变更目标

* 将“文章管理”改名为“公共知识库”，并使用 `Win11KnowledgeBase.jsx`

* 将“知识库管理”改名为“知识库”，并使用 `Win11KnowledgeFolderView.jsx`

* 将“我的知识”改名为“我的知识库”，并使用 `Win11MyKnowledgeBase.jsx`

## 修改范围

1. 更新侧边栏菜单文案与对应 tab id：

* 文件：`src/components/Sidebar.jsx`

* knowledge 分组下的 children：

  * `id: 'knowledge-articles'` 的 `label` 改为“公共知识库”

  * `id: 'knowledge-base'` 的 `label` 改为“知识库”

  * `id: 'my-knowledge'` 的 `label` 改为“我的知识库”

1. 更新 `App.jsx` 中 `renderContent` 的组件映射：

* 文件：`src/App.jsx`

* 对应 `case`：

  * `case 'knowledge-articles'` → 返回 `<Win11KnowledgeBase />`

  * `case 'knowledge-base'` → 返回 `<Win11KnowledgeFolderView />`

  * `case 'my-knowledge'` → 返回 `<Win11MyKnowledgeBase />`

* 保留或移除 `*win11` 备用映射不变（如需简化可后续移除）

## 验证

* 重启前端后检查侧边栏显示为“公共知识库 / 知识库 / 我的知识库”

* 进入每个菜单项后组件正确渲染：

  * 公共知识库 → `Win11KnowledgeBase`

  * 知识库 → `Win11KnowledgeFolderView`

  * 我的知识库 → `Win11MyKnowledgeBase`

## 备注

* 若仍显示旧文案，清理前端缓存并确保 Vite 热更新已应用。

