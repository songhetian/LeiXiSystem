{
  "name": "lei-system",
  "private": true,
  "version": "0.1.0",
  "description": "雷犀客服管理系统（重构版）— pnpm workspace",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev:frontend": "pnpm --filter @lei/frontend dev",
    "dev:backend": "pnpm --filter @lei/backend dev"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
